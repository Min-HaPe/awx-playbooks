---
- name: Update and upgrade apt packages
  hosts: all
  gather_facts: yes
  become: true
  become_method: sudo
  
  tasks:
    - name: Update apt package index
      apt:
        update_cache: yes

    - name: Upgrade all apt packages
      apt:
        upgrade: dist
